<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ian.mapper.SetmealDishMapper">


    <insert id="insert" parameterType="java.util.List">
        INSERT INTO setmeal_dish(setmeal_id, dish_id, name, price, copies, create_time, update_time, create_user, update_user)
        values
            <foreach collection="setmealDishList" item="item" index="i" open="" separator="," close="">
                (
                #{item.setmealId},#{item.dishId},#{item.name},#{item.price},#{item.copies}
                ,#{item.createTime},#{item.updateTime},#{item.createUser},#{item.updateUser}
                )
            </foreach>
    </insert>

    <delete id="deleteByIds"  parameterType="Long">
        delete
        from setmeal_dish
        where setmeal_id in
        <foreach collection="ids" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

</mapper>